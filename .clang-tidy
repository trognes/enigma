# clang-tidy configuration for the enigma tool.
#
# We run the bug-finding and performance check groups, but disable three checks
# that are pure noise for this deliberately C-style codebase:
#   * bugprone-easily-swappable-parameters - fires on the small-integer setting
#     APIs (init_walzen, init_ring_grund, ...) by design, not a bug.
#   * bugprone-narrowing-conversions - fires on every `char c = toupper(...)`
#     and similar intentional char/int arithmetic.
#   * performance-enum-size - wants a 1-byte base type for the 5-value scoring
#     enum; irrelevant for a config selector that is never stored in bulk.
# Findings in our own headers are reported (they are not by default); anything
# under a system include path stays suppressed. A header included by several
# translation units reports once per includer.
HeaderFilterRegex: 'src/.*'
Checks: >
  -*,
  bugprone-*,
  clang-analyzer-*,
  performance-*,
  -bugprone-easily-swappable-parameters,
  -bugprone-narrowing-conversions,
  -performance-enum-size
WarningsAsErrors: '*'
