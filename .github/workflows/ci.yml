name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Show compiler version
        run: g++ --version

      - name: Build
        run: make

      - name: Run tests
        run: make test
